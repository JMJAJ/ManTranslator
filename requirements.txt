# GUI
PySide6>=6.6.0

# Imaging / CV
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# OCR engines
manga-ocr>=0.1.11
paddleocr>=2.7.0
pytesseract>=0.3.10

# Inpainting (LaMa) + deep learning backend
torch>=2.0.0
torchvision>=0.15.0
# NOTE: simple-lama-inpainting is installed separately with --no-deps because
# its metadata pins pillow<10, which conflicts with manga-ocr (pillow>=10).
# It runs fine on Pillow 10. See README for the exact command.

# Vendored comic-text-detector (.repos/comic-text-detector) runtime deps
shapely>=2.0.0
pyclipper>=1.3.0
torchsummary>=1.5.1
tqdm>=4.41.0

# AI provider SDKs
openai>=1.30.0
google-generativeai>=0.7.0
anthropic>=0.30.0

# Utilities
requests>=2.31.0
